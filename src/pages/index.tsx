import { useSession } from 'next-auth/react'
import Head from 'next/head'
import Image from 'next/image'
import { ArrowRight } from 'phosphor-react'
import Link from 'next/link'
import logo from '../assets/beer-logo.png'

export default function Home() {
  const { data: session } = useSession()

  return (
    <>
      <Head>
        <title>Orders</title>
        <meta name='description' content='Generated by create next app' />
        <meta name='viewport' content='width=device-width, initial-scale=1' />
        <link rel='icon' href='/beer-logo.png' />
      </Head>
      <main className='font-poppins relative h-[75vh]
            flex flex-col items-center justify-center gap-8'>
          <Image src={logo} width={200} height={200} alt='' />
          <h1 className="text-4xl font-bold">SuuuperFast</h1>
          <Link href={'/home'}  className="flex items-center justify-center gap-2 font-bold  text-green-500">
            Ver Produtos 
            <ArrowRight size={22} />
          </Link>
      </main>
    </>
  )
}
